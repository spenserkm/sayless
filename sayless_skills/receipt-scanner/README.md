# Receipt Scanner Skill

This skill scans Gmail for purchase receipts and order confirmations, extracts structured purchase details, and exports results as a CSV.

## Files

- `SKILL.md`: Main skill definition (instructions, triggers, parsing workflow)
- `templates/`: Optional template files used by the skill
- `examples/`: Optional example inputs/outputs

## Notes

- Requires the Gmail connector to be enabled when running the skill.
